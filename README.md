# Splitwise CLI

A CLI interface for [Splitwise API](https://dev.splitwise.com/).

This is a personal and cut-down interface. You probably do not want to use it.
I built this to enable interoperability with apps in other languages (mainly
Haskell) that do not have a native Splitwise library.
